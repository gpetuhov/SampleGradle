package com.gpetuhov.android.gradleutils

// Provides file name that is used in build.gradle to name the artifact being built
class FileNameProvider {
    String getFileName() {
        "my_awesome_app.apk"
    }
}